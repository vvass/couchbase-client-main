# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Couchbase client routes or endpoints to retrieve info from couchbase server storage
GET       /index  controllers.TwitterController.index
GET      /postResponse/:id/:text  controllers.TwitterController.postResponse(id: Long,text: String)
GET       /getDoc  controllers.CouchbaseQueryController.getDocument
GET       /oldgGetDoc  controllers.CouchbaseQueryController.oldGetDocument

