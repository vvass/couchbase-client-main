# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Couchbase client routes or endpoints to retrieve info from couchbase server storage
GET       /getDoc  controllers.CouchbaseQueryControllerImp.getDocument
#GET       /newGetDoc/:id/:screenName/:text  controllers.CouchbaseQueryController.newGetDoc(id: Long, screenName: String, text: String)


